Linear Queue Implementation in C
This project implements a linear queue using an array in C. The queue supports basic operations such as initialization, enqueue, dequeue, checking if the queue is full or empty, and traversing the queue.
